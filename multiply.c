#include <stdio.h>
#include <conio.h>
int main()
{
    int a = 12, b = 10;
    printf("The product of 'a' and 'b' is  %d", a * b);
    printf("The product of 'a' and 'b' is  %d", a * b);
    printf("The product of 'a' and 'b' is  %d", a * b);
    return 0;
}